/**
 * Test IDs for DatasourceEditor.
 *
 * Inlined from @volkovlabs/components TEST_IDS.datasourceEditor.
 */
import { createSelector } from '../../test-utils/jest-selectors';

export const DATASOURCE_EDITOR_TEST_IDS = {
  fieldSelect: createSelector('data-testid datasource-editor field-select'),
};
